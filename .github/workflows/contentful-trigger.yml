name: Build and Deploy on Contentful Publish Event

on:
  repository_dispatch:
    types: [publish-event]

jobs:
  deploy:
    runs-on: ubuntu-18.04
    environment: production
    steps:
      - name: Log Action
        run: echo "Action Running."

